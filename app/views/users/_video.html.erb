<div class="video-wrapper">
    <div id="youtube-link">
        <% if youtube.nil? %>
            <em><%= I18n.t :users_no_videos %></em>
        <% else %>
            <div class="embed-container">
                <%= embed(youtube) %>
            </div>
        <% end %>
    </div>
</div>
