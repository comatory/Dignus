    <nav class="navbar navbar-default navbar-fixed-top">
  <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
          <%= link_to "Dignus", root_path, class: "navbar-brand" %>
    </div>
    <div id="navbar" class="navbar-collapse collapse">
      <ul class="nav navbar-nav">
      </ul>
      <ul class="nav navbar-nav navbar-right">
        <%= content_tag(:li, (link_to "Events", events_path)) %>
        <%= content_tag(:li, (link_to "Performers", performers_path)) %>
        <% if user_signed_in? && current_user.organizer %>
        <%= content_tag(:li, (link_to "Add event", new_user_event_path(current_user)) ) %>
        <% end %>

        <% if user_signed_in? %>
        <%= content_tag(:li, (link_to "Profile", user_path(current_user)) ) %>
        <%= content_tag( :li, link_to((I18n.t :sign_out), destroy_user_session_path, method: :delete) ) %>
        <% else %>
        <%= content_tag( :li, link_to((I18n.t :sign_in), new_user_session_path) ) %>
        <% end %>

      </ul>
    </div><!--/.nav-collapse -->
  </div>
</nav>
